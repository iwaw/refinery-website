<%= render 'refinery/admin/search_header', :url => refinery.conferences_admin_conferences_path %>
<div class='pagination_container'>
  <% if @conferences.any? %>
    <%= render 'conferences' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
