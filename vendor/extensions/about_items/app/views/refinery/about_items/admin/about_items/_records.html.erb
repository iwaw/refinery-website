<%= render 'refinery/admin/search_header', :url => refinery.about_items_admin_about_items_path %>
<div class='pagination_container'>
  <% if @about_items.any? %>
    <%= render 'about_items' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
